

    <div class="float-right mb-5 dark:text-white">
      <%= render 'update_status', project: @project %>
    </div>
    <div class="w-full p-4 text-center bg-white border border-gray-200 rounded-lg shadow-sm sm:p-8 dark:bg-gray-800 dark:border-gray-700">
      <h5 class="mb-2 text-3xl font-bold text-gray-900 dark:text-white"><%= @project.name %></h5>
      <p class="mb-5 text-base text-gray-500 sm:text-lg dark:text-gray-400"><%= @project.description %></p>

      <div class="flex flex-col justify-center overflow-y-scroll h-32 p-5">
      <% @project_activities.each do |activity| %>
          <%= render 'activities/activity', activity: activity %>
        <% end %>
      </div>
    </div>

